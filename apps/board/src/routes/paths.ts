export const LOGIN_PATH = '/login';

/**
 * The board's single screen (docs/design-brief.md, "Tablero de institución").
 * No further split — a kiosk has one signed-in `institution_member` and shows
 * exactly one institution's feed (ADR-068 point 1). Placeholder content only
 * this session; the real "llegadas de aeropuerto" board lands next.
 */
export const HOME_PATH = '/';

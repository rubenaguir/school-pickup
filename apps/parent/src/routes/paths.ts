export const LOGIN_PATH = '/login';

/**
 * Placeholder landing route. No business screen exists yet ("Mis hijos",
 * seguimiento, etc. — next session); this is only where a signed-in user
 * lands for now.
 */
export const HOME_PATH = '/';

Ítem de navegación para la sidebar navy (`--ink-900`) de portales de escritorio. El conteo (`count`) usa ámbar de estado, no coral — el coral queda para el estado activo.

```jsx
<NavItem icon={<HomeIcon/>} label="Instituciones" active count={7} onClick={goTo}/>
```

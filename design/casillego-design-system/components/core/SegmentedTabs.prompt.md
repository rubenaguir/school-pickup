Selector de pestañas/filtros en píldora. El activo usa fondo navy (`--ink-900`), nunca coral (el coral se reserva a la acción primaria).

```jsx
<SegmentedTabs options={['Carga','Vacío','Error','Con datos']} value={demo} onChange={setDemo} />
```

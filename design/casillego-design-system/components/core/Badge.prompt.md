Pastilla de estado. Usa siempre los tonos de estado (`en-route`, `arriving`, `arrived`, `delivered`, `cancelled`) para el sistema de recogida — nunca inventes otro color para estos 5.

```jsx
<Badge tone="arriving">Llegando</Badge>
<Badge tone="brand" dot={false}>Nuevo</Badge>
```
